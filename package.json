{
  "name": "superblocks-azure-token-exchange",
  "version": "1.0.0",
  "description": "AWS Lambda token exchange: validate Entra access token, return original token",
  "type": "module",
  "main": "handler.js",
  "scripts": {
    "sam:build": "sam build",
    "sam:local": "sam local start-api --env-vars env.local.json",
    "sam:invoke": "sam local invoke TokenExchangeFunction --env-vars env.local.json -e events/token-exchange.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
